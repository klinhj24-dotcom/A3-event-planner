{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "prepare": "test -d .git && git config core.hooksPath .githooks || true",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:vercel": "BASE_PATH=/ PORT=3000 pnpm -C artifacts/studio-hub run build && pnpm -C artifacts/api-server run build && rm -rf public && cp -r artifacts/studio-hub/dist/public ./public",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
